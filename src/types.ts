export interface ServiceCard {
  id: string;
  titulo: string;
  descricao: string;
}

export interface PortfolioPhoto {
  id: number;
  src: string;
  titulo: string;
  categoria: string;
}

export interface DifferenceBlock {
  id: string;
  titulo: string;
  descricao: string;
}
